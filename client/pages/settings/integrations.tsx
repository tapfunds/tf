import type { NextPage } from "next";
import { SettingsIntegrations } from "../../containers";

const IntegrationsPage: NextPage = () => {
  return <SettingsIntegrations />;
};

export default IntegrationsPage;
